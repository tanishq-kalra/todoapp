services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - redis
    environment:
      - MONGO_URL=mongodb+srv://todoadmin:strongpassword@cluster0.nvwohkt.mongodb.net/?appName=Cluster0
      - DB_NAME=tododb
      - JWT_SECRET=a1b2c3d4e5f6g7h8
      - JWT_ALGORITHM=HS256

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
  redis:
    image: redis:7
    ports:
      - "6379:6379"